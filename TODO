- Vim:
  - Try vim-classic?
  - vim-qf issues:
    - Use of `:Restore` with `:colder` is broken.
      - Overwrites other qf lists with old versions of different ones.
      - Not sure if possible to fix easily?  cfilter creates new entries
        instead of modifying old ones.
      - Make qf's `colder` and `cnewer` functions resize the window.
        - Contribute this as a fix upstream.
        - Missing a dedicated function to perform the resize.
      - Maybe switch to cfilter and custom code.
  - Replace `fd`?
  - Re-add Ascribe?
  - Interactive Vim commands
    - Finish arglist plugin.
    - Create an IBuffer-like interface in Vim for buffer management.
    - Interactive register interface in Vim.  Update/clear registers.
    - Dired-like interface in Vim for file management?
      - <https://www.are.na/block/50371058>
    - Package into a plugin and share?

- Tmux:
  - <https://jkrl.me/2025/11/28/suspending-shell-processes.html>
  - <https://www.brain-dump.org/projects/abduco/>
    - Set environment variable to place session info into `~/.local/state/abduco/`?
      - Homebrew version is out of date.
    - Replace tmux-project script using abduco?
      - Write in execline.
      - `abduco -e ^q -f -A session-name command`
      - recursively call back to itself upon exit?
    - Use for background jobs in Vim to continue running while Vim is suspended?
      - `call axvr#BackgroundJob('abduco -e ^q -A website-server do/serve')`
        - Or maybe the `do/serve` script should do this?

- More:
  - Install execline and s6 man pages.  <https://sr.ht/~humm/skarnet-man-pages/sources>
  - Start using s6 running in user space?
    <https://lobste.rs/s/evnx8i/s6_skarnet_s_small_supervision_suite#c_h6vwgn>
