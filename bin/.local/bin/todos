#!/usr/bin/env -S execlineb -W

# List TODOs and other comment tags in specified files or a git repo.

multisubstitute
{
    elgetpositionals
    define regex "\\b(TO[-_ ]?DO|FIX[-_ ]?ME|XXX|BUG|HACK)\\b"
}
rg --vimgrep --hidden -g "!.git/*" -e $regex -- $@
